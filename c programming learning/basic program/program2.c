#include<stdio.h>
   int main(void)
   {
    int x=3,y=5;
    if(x==y)
        printf("%d %d",x,y);
    return 0;
   }
   
 

 //output:  the printf statement is not executed, and the program simply returns 0 without printing anything to the console.